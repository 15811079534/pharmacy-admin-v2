# 全模块写入测试报告

## 测试时间
- 2026-03-05 18:59:22（Asia/Shanghai）

## 测试脚本
- `./test-all-modules.sh`
- 后端地址：`http://localhost:48080/admin-api`
- App 地址：`http://localhost:48080/app-api`
- 测试账号：`admin`
- 租户：`tenant-id=1`
- 批量规模（本次执行）：`BULK_COUNT=5`
- App 批量参数（本次执行）：`MEMBER_BATCH_COUNT=3`、`ORDER_BATCH_COUNT=5`
- 可选细粒度参数：`GOODS_BATCH_COUNT`、`STORE_BATCH_COUNT`、`BANNER_BATCH_COUNT`、`NOTICE_BATCH_COUNT`、`CATEGORY_BATCH_COUNT`、`BRAND_BATCH_COUNT`、`MEMBER_BATCH_COUNT`、`ORDER_BATCH_COUNT`、`MEMBER_MOBILES_CSV`、`APP_ORDER_SKU_ID`

## 结果汇总
- 总计：74 项
- 通过：74 项
- 失败：0 项
- 跳过：0 项
- 成功率：100.0%
- App 写入：地址 3 条、订单 15 条
- 结论：所有模块已完成“真实接口写入 + 校验”；后台测试数据自动清理，App 地址/订单保留用于联调

## 写入测试覆盖
- 认证模块：登录获取 Token
- 药品管理：新增 1 个一级分类 + 5 个二级分类 + 5 个品牌 + 5 个药品
- 库存管理：对 5 个药品分别执行入库（库存+5）、出库（库存-3）
- 医疗业务：新增 5 个门店
- 营销管理：新增 5 个轮播图、5 个公告
- App 会员/订单：3 个会员登录、3 条收货地址写入、15 条订单写入（`/app-api/trade/order/create`）

## 查询校验覆盖
- 药品管理：分类列表、药品分页
- 订单管理：订单分页、售后分页、物流公司列表
- 医疗业务：处方分页、医保分页、门店列表
- 会员管理：会员分页
- 营销管理：轮播图分页、公告分页

## 清理机制验证
- 自动清理成功：公告、轮播图、门店、药品、品牌、二级分类、一级分类（均按批量倒序删除）
- 药品清理流程：先更新状态到回收站（`/product/spu/update-status`），再执行删除（`/product/spu/delete`）

## 本次修复项
- 修复分类 API 字段映射：`icon` ↔ `picUrl`，补齐 `parentId`
  - 文件：`src/api/goods/category.ts`
- 修复轮播图 API 字段映射：`image/linkValue` ↔ `picUrl/url`，补齐 `position`
  - 文件：`src/api/marketing/banner.ts`
- 药品表单改为动态加载二级及以下分类（符合后端分类层级约束）
  - 文件：`src/views/goods/goods/GoodsForm.vue`
- 升级全模块脚本为写入测试并增强库存与清理逻辑
  - 文件：`test-all-modules.sh`
- 增加 App 批量写入链路（会员登录、地址新增、订单新增）
  - 文件：`test-all-modules.sh`
- 增加商品表兼容 SQL 补丁（解决 `product/spu/create` 的旧库字段约束问题）
  - 文件：`../pharmacy-server/sql/mysql/product_schema_compat_patch.sql`
- 增加交易/会员链路兼容 SQL 补丁（member/trade/pay 最小可用结构）
  - 文件：`../pharmacy-server/sql/mysql/trade_member_schema_compat_patch.sql`

## 构建验证
- 执行：`npm run build`
- 结果：通过
